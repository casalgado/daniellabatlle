<h1><%= t('wholesale_signup') %></h1>

<%= form_for @wholesaler do |form| %>
  <%= render "spree/shared/error_messages", :target => @wholesaler %>
  <%= render 'fields', :form => form %>

  <p class="action">
    <%= form.submit Spree.t("wholesale_apply", default: "Apply"), :class => 'button' %>
  </p>
<% end %>
